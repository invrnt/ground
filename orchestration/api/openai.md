# Historical OpenAI transport

The user requested migration to OpenRouter. The active contract is [OpenRouter](openrouter.md). Earlier handoffs and stored responses retain their original OpenAI attribution. No OPENAI_* variable enables the current interpretation workflow.
